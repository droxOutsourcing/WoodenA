{
  "name": "wooden-a-complete",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm run build && mkdir -p ../dist/public && cp -r dist/* ../dist/public/",
    "start": "echo 'WoodenA Complete Project Ready'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.50.5"
  }
}